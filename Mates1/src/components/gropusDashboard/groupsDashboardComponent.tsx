import React, { Component } from "react";
import { StyleSheet, Text, View } from "react-native";

export default class gropusDashboard extends Component {
  render() {
    return <Text>LOGIN</Text>;
  }
}
